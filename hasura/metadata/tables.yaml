- table:
    schema: public
    name: todos
  event_triggers:
    - name: todos-capitalize
      definition:
        enable_manual: false
        insert:
          columns: "*"
      retry_conf:
        num_retries: 0
        interval_sec: 10
        timeout_sec: 60
      webhook_from_env: EVT_TODOS_CAPITALIZE_URL
